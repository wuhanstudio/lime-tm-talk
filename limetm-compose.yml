version: "3.3"

services:
  nginx:
    image: nginx:stable-alpine
    volumes:
      - ./docs:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/nginx.conf
    deploy:
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.limetm.rule=Host(`lime-tm.wuhanstudio.uk`)"
        - "traefik.http.routers.limetm.entrypoints=web"
        - "traefik.http.services.limetm.loadbalancer.server.port=80"
        # TLS
        - "traefik.http.routers.limetms.rule=Host(`lime-tm.wuhanstudio.uk`)"
        - "traefik.http.routers.limetms.entrypoints=websecure"
        - "traefik.http.routers.limetms.tls.certresolver=myhttpchallenge"
        # Redirect
        - "traefik.http.routers.limetm.middlewares=https_redirect"
        - "traefik.http.middlewares.https_redirect.redirectscheme.scheme=https"
    networks:
      - traefik-public
networks:
  traefik-public:
    external: true
